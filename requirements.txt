# Pinned runtime dependencies for a reproducible environment.
# The canonical dependency source is pyproject.toml; this file pins the API +
# core-science stack for `pip install -r requirements.txt`.
numpy==1.26.4
scipy==1.13.1
pandas==2.2.2
pyarrow==16.1.0
duckdb==1.0.0
fastapi==0.111.0
pydantic==2.7.1
uvicorn==0.30.1
httpx==0.27.0

# Dashboard (optional): pip install -r requirements.txt streamlit plotly
# streamlit==1.36.0
# plotly==5.22.0
