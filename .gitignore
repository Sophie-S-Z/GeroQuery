# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Full real NHANES table: reproducible from the pinned manifest via `make data`.
# Committing it would ship a second, unverifiable copy. The seeded 600-row
# sample (nhanes_2017_2018_sample.csv) IS committed so tests run offline.
geroquery/sources/data/clinical_nhanes_full.csv

# GeroQuery runtime build & cache (regenerated from bundled fixtures)
geroquery/_build/
geroquery/_cache/
_build/
_cache/

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Node / frontend
frontend/node_modules/
frontend/dist/

# OS / editors
.DS_Store
.idea/
.vscode/

# Derived tables: reproducible from the pinned manifest, so a committed copy
# would be a second, unverifiable one. The committed samples beside them are the
# offline path (see docs/DATA_SOURCES.md).
geroquery/sources/data/clinical_nhanes_full.csv
geroquery/sources/data/signatures_full.csv

# Stray root lockfile: the only npm project is frontend/, which has its own.
/package-lock.json
